/**
 * Dispatch algorithm v1 scorer — implementation lives in `calculateMatchScore.ts`.
 * Kept as a stable import path for ALGORITHMS.md / admin code.
 */
export {
  arrivalWindowFromScheduledAt,
  calculateMatchScore,
  computeMatchScore,
} from "@/lib/matching/calculateMatchScore";
